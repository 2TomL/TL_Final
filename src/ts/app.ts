// TypeScript entry point for navbar and page logic
window.addEventListener('DOMContentLoaded', () => {
  if (window.setupNavbar) window.setupNavbar();
  if (window.setupSoundCloudPlayer) window.setupSoundCloudPlayer();
});
